const longueur = Number(prompt("Entrer la longueur du rectangle:"));
const largeur = Number(prompt("Entrer la largeur du rectangle:"));

P = 2 * (longueur + largeur);

console.log("Le périmètre du rectangle est:", P);
